# منصة الرسم الجماعي 🎨

منصة احترافية للرسم الجماعي والتحديات الفنية مع دعم كامل لـ HTTPS وSSH.

## ✨ المميزات الرئيسية

### 🔐 الأمان
- ✅ HTTPS فقط في الإنتاج
- ✅ تشفير جميع الاتصالات (SSL/TLS)
- ✅ إدارة السيرفر عبر SSH
- ✅ JWT Tokens للمصادقة
- ✅ Rate Limiting
- ✅ Helmet.js للحماية

### 👤 نظام الحسابات
- ✅ تسجيل الدخول والتسجيل
- ✅ تسجيل دخول Google (جاهز للتكامل)
- ✅ ملف شخصي كامل
- ✅ محفوظات الرسومات
- ✅ نظام التقييمات

### 🏠 نظام الغرف
- ✅ إنشاء غرف بأكواد فريدة
- ✅ الانضمام عبر الكود
- ✅ إعدادات قابلة للتخصيص
- ✅ مؤقت للتحديات
- ✅ قائمة المشاركين

### 🎨 لوحة الرسم
- ✅ Canvas منفصل لكل مستخدم (محمي)
- ✅ لا يمكن تعديل رسومات الآخرين
- ✅ يمكن نسخ رسومات الآخرين
- ✅ أدوات كاملة (قلم، ممحاة، ألوان)
- ✅ حفظ تلقائي
- ✅ دعم اللمس للجوال

### ⭐ نظام التقييم
- ✅ تقييم الرسومات بعد انتهاء الوقت
- ✅ ترتيب الفائزين
- ✅ عرض أفضل 3 رسومات
- ✅ إحصائيات مفصلة

## 🚀 التثبيت والتشغيل

### 1. تثبيت الحزم

```bash
npm run install-all
```

### 2. إعداد متغيرات البيئة

**Backend (`server/.env`):**
```env
PORT=3000
NODE_ENV=development
CLIENT_URL=http://localhost:5173
JWT_SECRET=your-secret-key
HTTPS_ENABLED=false
```

**Frontend (`client/.env`):**
```env
VITE_API_URL=http://localhost:3000
VITE_SOCKET_URL=http://localhost:3000
```

### 3. التشغيل

```bash
npm run dev
```

- السيرفر: `http://localhost:3000`
- الواجهة: `http://localhost:5173`

## 📦 النشر على السيرفر

### إعداد HTTPS

1. احصل على شهادات SSL (Let's Encrypt):
```bash
sudo certbot certonly --standalone -d your-domain.com
```

2. أضف المسارات في `server/.env`:
```env
HTTPS_ENABLED=true
SSL_KEY_PATH=/etc/letsencrypt/live/your-domain.com/privkey.pem
SSL_CERT_PATH=/etc/letsencrypt/live/your-domain.com/fullchain.pem
```

3. بناء الواجهة:
```bash
cd client
npm run build
```

4. تشغيل السيرفر:
```bash
cd server
npm start
```

### استخدام PM2

```bash
npm install -g pm2
pm2 start server/index.js --name drawing-app
pm2 save
pm2 startup
```

## 📁 البنية

```
├── server/              # Backend
│   ├── routes/          # API routes
│   ├── middleware/       # Auth middleware
│   ├── socket.js         # WebSocket handlers
│   └── data/            # Database (JSON)
├── client/              # Frontend
│   └── src/
│       ├── pages/       # الصفحات
│       ├── components/  # المكونات
│       └── context/     # Context API
└── package.json
```

## 🔒 الأمان

- جميع الاتصالات مشفرة (HTTPS)
- JWT Tokens للمصادقة
- Rate Limiting
- Helmet.js للحماية
- CORS محدود
- التحقق من المدخلات

## 📱 المتطلبات

- Node.js 16+
- npm أو yarn

## 🎯 الاستخدام

1. سجل الدخول أو أنشئ حساب
2. أنشئ غرفة جديدة أو انضم بكود
3. ابدأ الرسم والتحدي
4. قيّم الرسومات بعد انتهاء الوقت
5. شاهد النتائج والترتيب

## 📝 ملاحظات

- البيانات محفوظة في ملفات JSON (يمكن ترقية لقاعدة بيانات)
- تسجيل دخول Google يحتاج إعداد OAuth
- في الإنتاج، استخدم HTTPS دائماً
- غيّر JWT_SECRET في الإنتاج

## 🔄 التطوير المستقبلي

- [ ] قاعدة بيانات حقيقية (MongoDB/PostgreSQL)
- [ ] تسجيل دخول Google كامل
- [ ] رفع الصور الشخصية
- [ ] نظام إشعارات
- [ ] غرف عامة وخاصة
- [ ] تاريخ التحديات

## 📄 الترخيص

MIT
